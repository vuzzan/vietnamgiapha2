<mah:MetroWindow x:Class="vietnamgiapha.AI.AiChatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Trợ lý AI Gia phả"
        Height="600" Width="560"
        MinHeight="400" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Bubble người dùng -->
            <Style x:Key="UserBubble" TargetType="Border">
                <Setter Property="Background" Value="#2E5FA3"/>
                <Setter Property="CornerRadius" Value="12,12,2,12"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Margin" Value="60,4,4,4"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="MaxWidth" Value="380"/>
            </Style>
            <!-- Bubble AI -->
            <Style x:Key="AiBubble" TargetType="Border">
                <Setter Property="Background" Value="#F0F4FA"/>
                <Setter Property="CornerRadius" Value="12,12,12,2"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Margin" Value="4,4,60,4"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="MaxWidth" Value="380"/>
                <Setter Property="BorderBrush" Value="#D0D8EA"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <!-- TextBox read-only dùng trong bubble — bỏ toàn bộ chrome mặc định -->
            <Style x:Key="BubbleTextBoxStyle" TargetType="TextBox">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Disabled"
                                          VerticalScrollBarVisibility="Disabled"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E5FA3" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🤖" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Trợ lý AI Gia phả" FontSize="13" FontWeight="SemiBold"
                                   Foreground="White"/>
                        <TextBlock x:Name="statusText" Text="Sẵn sàng" FontSize="10"
                                   Foreground="#B0C8F0"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="clearBtn" Content="Xóa lịch sử" Padding="8,4"
                            Foreground="White" Background="Transparent"
                            BorderBrush="#5080C0" BorderThickness="1"
                            Click="ClearBtn_Click" Margin="0,0,8,0" FontSize="11"/>
                    <Button x:Name="settingsBtn" Content="⚙" Width="28" Height="28"
                            ToolTip="Cài đặt AI" Background="Transparent"
                            BorderBrush="#5080C0" BorderThickness="1"
                            Foreground="White" Click="SettingsBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Vùng chat -->
        <ScrollViewer x:Name="chatScroll" Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="4,4,4,0">
            <StackPanel x:Name="chatPanel" Margin="0,4"/>
        </ScrollViewer>

        <!-- Input -->
        <Border Grid.Row="2" BorderBrush="#D0D8E8" BorderThickness="0,1,0,0"
                Background="White" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="inputBox"
                         AcceptsReturn="False"
                         TextWrapping="Wrap"
                         MaxLines="4"
                         MinHeight="36"
                         MaxHeight="80"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Center"
                         Padding="8,6"
                         FontSize="13"
                         Margin="0,0,8,0"
                         mah:TextBoxHelper.Watermark="Hỏi về gia phả... (Enter để gửi)"
                         KeyDown="InputBox_KeyDown"/>
                <Button Grid.Column="1" x:Name="sendBtn"
                        Content="Gửi ▶" Width="72" Height="36"
                        Click="SendBtn_Click"
                        Background="#2E5FA3" Foreground="White"
                        BorderThickness="0" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</mah:MetroWindow>
