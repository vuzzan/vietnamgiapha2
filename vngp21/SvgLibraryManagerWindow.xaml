<mah:MetroWindow x:Class="vietnamgiapha.SvgLibraryManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:vietnamgiapha"
        Title="Quản lý SVG zone (title / family)"
        Height="720"
        Width="980"
        MinHeight="520"
        MinWidth="820"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Orange.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <HierarchicalDataTemplate DataType="{x:Type local:SvgTreeNode}" ItemsSource="{Binding Children}">
                <TextBlock Text="{Binding DisplayText}" TextWrapping="Wrap"/>
            </HierarchicalDataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Margin="0 0 0 8">
            <Button x:Name="btnRefresh" Content="Tải lại cloud" Width="88" Click="Refresh_Click"/>
            <Button x:Name="btnImportToLocal" Content="→ Thêm vào kho local" Width="130" Margin="4 0 0 0"
                    Click="ImportToLocal_Click" ToolTip="Tải khung cloud đang chọn vào file gia phả (tăng lượt tải)"/>
            <Separator Margin="8 0"/>
            <Button x:Name="btnNew" Content="Tạo mới" Width="72" Margin="4 0 0 0" Click="New_Click"/>
            <Button x:Name="btnLoadFile" Content="Tải file .svg" Width="95" Margin="4 0 0 0" Click="LoadFile_Click"/>
            <Button x:Name="btnSaveZone" Content="Lưu file SVG" Width="100" Margin="4 0 0 0" Click="SaveToZoneFolder_Click"
                    Style="{StaticResource MahApps.Styles.Button.Square.Accent}"
                    ToolTip="Lưu vào thư mục svgZone / ZoneSvg (title_*.svg hoặc family_*.svg)"/>
            <Button x:Name="btnUpload" Content="Upload cloud" Width="95" Margin="4 0 0 0" Click="Upload_Click"/>
            <Separator Margin="8 0"/>
            <TextBlock Text="Loại:" VerticalAlignment="Center" Margin="4 0 4 0"/>
            <ComboBox x:Name="uploadCategoryCombo" Width="100" IsEditable="False"/>
            <TextBlock Text="Tên file:" VerticalAlignment="Center" Margin="8 0 4 0"/>
            <TextBox x:Name="uploadNameBox" Width="140" mah:TextBoxHelper.Watermark="phần sau title_ / family_"/>
            <TextBlock Text="Tác giả:" VerticalAlignment="Center" Margin="8 0 4 0"/>
            <TextBox x:Name="uploadAuthorBox" Width="90" mah:TextBoxHelper.Watermark="Tác giả"/>
        </ToolBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="220"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#CCC" BorderThickness="1" Padding="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0">
                        <TextBlock DockPanel.Dock="Top" Text="Kho cloud (title / family)" FontWeight="SemiBold" Margin="0 0 0 4"/>
                        <TreeView x:Name="svgTree"
                                  ItemsSource="{Binding TreeRoots}"
                                  SelectedItemChanged="SvgTree_SelectedItemChanged"/>
                    </DockPanel>

                    <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" Background="#E0E0E0"/>

                    <DockPanel Grid.Row="2">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 4">
                            <TextBlock Text="Thư mục zone (SVG)" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Content="↻" Width="28" Height="22" Margin="6 0 0 0" Click="RefreshLocal_Click"
                                    ToolTip="Tải lại file title_* / family_* trong thư mục SVG"/>
                        </StackPanel>
                        <ListBox x:Name="localSvgList"
                                 DisplayMemberPath="DisplayText"
                                 ItemsSource="{Binding LocalItems}"
                                 SelectionChanged="LocalSvgList_SelectionChanged"/>
                    </DockPanel>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" Background="Transparent"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0">
                    <TextBlock DockPanel.Dock="Top" Text="Mã SVG" FontWeight="SemiBold" Margin="0 0 0 4"/>
                    <TextBox x:Name="svgCodeBox"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="11"
                             TextChanged="SvgCodeBox_TextChanged"/>
                </DockPanel>

                <GridSplitter Grid.Row="1" Height="8" HorizontalAlignment="Stretch" Background="Transparent"/>

                <Border Grid.Row="2" BorderBrush="#CCC" BorderThickness="1" Background="#FAFAFA" Padding="4">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Xem trước" FontWeight="SemiBold" Margin="0 0 0 4"/>
                        <TextBlock x:Name="detailStatusText" DockPanel.Dock="Bottom" Margin="0 4 0 0"
                                   TextWrapping="Wrap" Foreground="Gray"/>
                        <Grid x:Name="previewHost" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>

        <TextBlock x:Name="statusBarText" Grid.Row="2" Margin="0 8 0 0" Foreground="Gray" TextWrapping="Wrap"/>
    </Grid>
</mah:MetroWindow>
